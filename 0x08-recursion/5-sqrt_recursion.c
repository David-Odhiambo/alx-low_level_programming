#include "main.h"

/**
 * _sqrt_recursion - return the natural square root of a number.
 * @n: number.
 * @r: natural square root.
 *
 * Return: -1 success.
 */
int _sqrt_recursion(int n);
{
	if (n = r)
		return (-1);
	if (n == r)
		return (0);
}
